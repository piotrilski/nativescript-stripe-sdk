/* Include.gradle configuration: http://docs.nativescript.org/plugins/plugins#includegradle-specification */

android {
	productFlavors {
		"nativescript-stripe-sdk" {
			dimension "nativescript-stripe-sdk"
		}
	}
}

dependencies {
	dependencies {
		compile 'com.stripe:stripe-android:5.1.0'
	}
}